# Family Frame

Interactive family chronicle prototype built with React, TypeScript, Vite, and Tailwind CSS.

```sh
cd "vishnia design"
npm install
npm run dev
```

Quality checks:

```sh
npm run lint
npm run build
```
